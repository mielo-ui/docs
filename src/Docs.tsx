import * as Mie from "mielo-react"

export function DocsPage() {
  return (
    <div className="page checkbox">
      <Mie.Clamp
        header={
          <Mie.Header
            subtitle="Getting started with Mielo UI"
            title="Mielo UI"
            size="massive"
            center
          />
        }
      >

      </Mie.Clamp>
    </div>
  )
}